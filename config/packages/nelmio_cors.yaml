nelmio_cors:
    defaults:
        origin_regex: true
        allow_origin: ['^https://nuvia-frontend.*\.vercel\.app$', '^http://localhost:5173$']
        allow_credentials: false
        allow_headers: ['Content-Type', 'Authorization', 'X-Requested-With']
        allow_methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS']
        max_age: 3600

    paths:
        '^/': ~